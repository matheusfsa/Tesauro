code teste
start
	unalterable symbol vector[19] txt = "ABABDABACDABABCABAB";
	unalterable integer i;
	integer m, l, r;
	integer vector[10] x;
	real a;
	a := 1;
	x[0] := 3;
	l := 3;
	r := 'c';
	capture(i);
	in case that (a < 3) do
		start
			as long as (l > 2) do
				start
					real m;
		  			m := l+r /2;
	  			finish
		finish
	considering x[0] from 0 to 10 by 1 do
		m := l+r /2;
		
finish